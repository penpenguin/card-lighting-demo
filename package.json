{
  "name": "card-lighting-demo",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "predev": "npm run bundle",
    "dev": "astro dev",
    "prebuild": "npm run bundle",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "bundle": "esbuild src/scripts/card-lighting.ts --bundle --format=esm --target=es2020 --outfile=public/scripts/card-lighting.js",
    "test": "vitest",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.16.5",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
